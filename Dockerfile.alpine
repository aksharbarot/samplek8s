# Dockerfile.vulnerable
FROM alpine:3.18

# Intentionally using outdated packages to simulate CVEs
RUN apk add --no-cache \
    openssl=3.0.9-r2 \
    curl=8.0.1-r0

# Simulated secret for Trivy's secret scanner
ENV AWS_SECRET_ACCESS_KEY="FAKEKEY123456"

CMD ["openssl", "version"]
